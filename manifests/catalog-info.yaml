apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.component_id | dump }}-${{ values.branch | dump }}
  {%- if values.description %}
  description: ${{ values.description | dump }}
  {%- endif %}
spec:
  type: service
  lifecycle: development
  owner: ${{ values.owner | dump }}
